# RSS Dashboard AI Contribution Instructions

This file is a quick policy card for AI-assisted edits.

Codex sessions must also follow `AGENTS.md`, `eslint.config.mjs`, and
`docs/development/test_coverage/testing-guide.md` before making code or test
changes. The ESLint configuration is authoritative for platform and coding
rules; resolve violations through compliant code, not rule suppression.

## Compliance Declarations (Required)

1. Never add unsafe HTML injection in production paths. Do not use direct `innerHTML` for feed/article rendering; use approved sanitizing append utilities.
2. Do not add `eslint-disable` without an inline reason. Prefer refactoring over suppression.
3. Avoid `as any` sprawl. Use one boundary cast (`as unknown as TypedInterface`) and keep the rest strongly typed.
4. In popout-sensitive UI code, prefer `activeDocument` over global `document`. Use `window.setTimeout/clearTimeout` and other `window.*` timers instead of `activeWindow.*` or bare timers, as enforced by `npm run check:platform`.
5. Follow Obsidian DOM helper conventions in production code (`createDiv`, `createEl`, `createSpan`, `createFragment`) unless a documented exception applies.
6. Follow existing restricted-import guidance and approved API alternatives.
7. Do not add CSS `!important` declarations. Use scoped, higher-specificity selectors; `audit-ok` is not an exception.
8. Give hover tooltips with `setTooltip(el, text)` from `obsidian` (or an `aria-label` in `attr`), never a `title` attribute. Obsidian draws its tooltip from `aria-label`, so `title` shows a second, browser-drawn popup, or an unthemed one on its own. ESLint rejects `title` set through `setAttr`/`setAttribute`, `attr: { title }`, `createEl`-family options, and `.title =` on element-named variables. It cannot see a `title` on an element nested inside one with an `aria-label`, so check nesting yourself. To update a tooltip, call `setTooltip` again; to clear it, remove `aria-label`.

## Pre-PR Checks

- Run `npm run lint`.
- Run `npm run test:unit` (or targeted tests with rationale).
- Run `npm run check:important` for CSS or CSS-policy changes.
- Confirm no new undocumented suppressions.

## Canonical References

- `AGENTS.md` (Codex workflow and validation requirements)
- `eslint.config.mjs` (authoritative lint and platform-compatibility rules)
- `docs/development/test_coverage/testing-guide.md` (test design and execution requirements)
- `CONTRIBUTING.md` (Compliance Declarations)
- `docs/development/compliance-patterns.md` (approved implementation patterns)
- `docs/plugin-scorecard.md` (current warning/error backlog)
