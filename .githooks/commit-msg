#!/bin/sh

if [ "$SKIP_GIT_HOOKS" = "1" ]; then
  echo "[commit-msg] SKIP_GIT_HOOKS=1 set, skipping."
  exit 0
fi

echo "[commit-msg] Commit message format validation disabled (repo uses ESLint checks only)."
exit 0
