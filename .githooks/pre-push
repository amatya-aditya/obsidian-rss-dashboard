#!/bin/sh

if [ "$SKIP_GIT_HOOKS" = "1" ]; then
  echo "[pre-push] SKIP_GIT_HOOKS=1 set, skipping."
  exit 0
fi

echo "[pre-push] Running full build gate..."
npm run --silent build
if [ $? -ne 0 ]; then
  exit 1
fi

echo "[pre-push] Running full unit suite..."
npm run --silent test:unit
