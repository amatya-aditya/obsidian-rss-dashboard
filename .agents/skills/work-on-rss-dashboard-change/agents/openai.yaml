interface:
  display_name: "Work on RSS Dashboard Change"
  short_description: "Plan, build, validate, and archive RSS Dashboard changes"
  default_prompt: "Use $work-on-rss-dashboard-change to implement this RSS Dashboard feature or bug, validate it, and close its matching plan."

policy:
  allow_implicit_invocation: true
